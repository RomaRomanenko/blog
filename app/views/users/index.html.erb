<p><%= link_to_modal 'Add New User', new_user_path %></p>
<table>
	<thead>
    	<tr>
    		<th>First Name</th>
			<th>Last Name</th>
			<th>Email</th>
			<th>Show</th>
			<th>Edit</th>
			<th>Destroy</th>  
    	</tr>
	</thead>
	<tbody>
    	<%- @users.each do |user| %>
    		<tr>
				<td><%= user.first_name %></td>
				<td><%= user.last_name %></td>
				<td><%= user.email %></td>
				<td><%= link_to_modal 'Show', user_path(user) %></td>
				<td><%= link_to_modal 'Edit', edit_user_path(user) %></td></td>
				<td><%= link_to 'delete', user_path(user), method: :delete,  data: { confirm: 'You sure?' } %></td>
    		</tr>
		<% end %>
	</tbody>
</table>